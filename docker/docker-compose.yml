services:
  my-heroes:
    build:
      context: ..
      dockerfile: ../Dockerfile
    container_name: my-heroes
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    env_file:
      - ./.env
    restart: unless-stopped
